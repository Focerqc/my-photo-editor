# Antigravity Photo Editor

A custom editor extension for cropping images directly in Antigravity.

Open any `.png`, `.jpg`, or `.jpeg` file and use the built-in crop tool to trim, zoom, pan, and save — all without leaving the editor.
